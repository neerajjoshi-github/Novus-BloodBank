import { Button } from "antd";
const App = () => {
  return (
    <div>
      <h1 className="text-primary">Novus BloodBank</h1>
      <Button className="font-bold" size="large" type="primary">
        Primary Button
      </Button>
    </div>
  );
};

export default App;
